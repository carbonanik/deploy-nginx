pipeline {
    agent any
    stages {
        stage('verifing') {
            steps {
                sh '''
                docker version
                '''
            }
        }
        stage('start nginx') {
            steps {
                sh 'docker compose -f deploy-nginx/docker-compose.yml up -d --build'
                sh 'docker compose ps'
            }
        }
    }
}